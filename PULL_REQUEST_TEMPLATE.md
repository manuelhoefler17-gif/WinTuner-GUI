## 🚀 Release v0.9.0

This PR merges all changes from the `Test` branch into `main`.

### What's included

| Phase | Version | Changes |
|-------|---------|---------|
| Phase 1 | v0.7.1 | Critical bugfixes: `$script:wingetVersionCache` init, BGW Dispose, thread-safe logging |
| Phase 2+3 | v0.8.0 | SHA256 self-update integrity check, log box 120px, tooltips, username persistence |
| Phase 4 | v0.8.1 | Winget version disk cache (JSON, TTL 6h), `Get-PreviousWingetVersion` refactor, cache-clear button |
| Phase 5 | v0.9.0 | English comments, PS-approved verb names (`Set-GuiTheme`, `Switch-GuiTheme`, `Resolve-WingetIdForApp`), central config block |